# Database Configuration
# For production: Use Neon PostgreSQL connection string
# For local development: Use SQLite (sqlite:///./test.db)
#DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/dbname?sslmode=require

# Authentication Configuration
# Generate a secure secret key with: openssl rand -hex 32
#AUTH_SECRET_KEY=your-secret-key-here-min-32-chars-change-in-production

# Application Configuration
#APP_NAME=Phase 2 Todo API
#DEBUG=false
#CORS_ORIGINS=*

# Note: In production, set specific CORS origins instead of "*"
# Example: CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
